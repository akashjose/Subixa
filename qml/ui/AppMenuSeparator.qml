import QtQuick
import QtQuick.Templates as T
import Subixa

T.MenuSeparator {
    padding: Theme.space.xs
    contentItem: Rectangle {
        implicitHeight: Theme.stroke.hairline
        color: Theme.color.borderSubtle
    }
}
